---
schema: freedom-path-todo/v0.1
project_id: ai-poster-text-editor
title: AI 海报文字快改
status: active
updated: 2026-08-26
freedom_paths: []
---

# AI 海报文字快改：项目待办

## 项目目标

提供一个本地优先、无需后端、默认中文的双语编辑器，让用户快速修改 AI 海报或封面的文字，同时保持背景图、排版数据和用户文件可控、可复用、可离线。

## 当前里程碑

`M-003` 稳定 AI 排版输入兼容层，明确片段导入语义，并准备下一次小版本发布。

## 当前

当前无进行中任务。

## 以后

- [ ] `TASK-APT-012` 评估并实现 Tauri 原生套壳
  - 完成标准：先提交独立技术方案与三平台构建边界；用户批准后再实现 Windows、macOS、Linux 安装包，且网页/PWA 版本继续可独立运行。
  - 状态：暂停，欢迎外部贡献者接手。

- [ ] `TASK-APT-013` 扩充 AI layout 兼容样本库
  - 完成标准：每种新增格式保留匿名化原始样本、预期归一化结果和端到端回归；解析器继续禁止代码执行。

## 证据

- 已发布版本：GitHub Release `v0.3.0`。
- 已发布在线版：<https://7panni.github.io/ai-poster-text-editor/>。
- 本地兼容性测试：`test/gpt-layout.txt`、`test/js-layout.txt`、`test/html-fragment-layout.txt` 均由 `test/visual-test.js` 覆盖。
- 2026-08-26 本轮验证：`pnpm run build` 成功；端到端测试覆盖空白启动、完整替换、片段按 id 合并、空数组拒绝、清空撤销、背景撤销后刷新、背景恢复、清除会话并严格删除记录、IndexedDB 损坏回退，全部通过（exit 0）。
- 启动默认图：`createParchment()` 已删除，初始化改为从 IndexedDB `session` store 恢复。
- 当前本机可直接使用 pnpm 运行构建与测试。
- 自动测试已改用 Playwright 专用 Chrome Headless Shell，并以 `finally` 保证异常时关闭，不再启动系统 Chrome 或产生 Dock 图标。
- 右侧属性区回归：960px 高度无需滚动即可看到 X/Y、删除当前文字和清空全部文字；顶部按钮增至 38px，900px 宽度仍不溢出。

## 关联与依赖

- 项目交接：`项目交接.md`
- 已完成事实：`项目已完成.md`
- 对外说明：`README.md`、`使用说明.md`
- Node 依赖：`package.json`、`pnpm-lock.yaml`
- 发布目标：GitHub Pages、GitHub Releases
