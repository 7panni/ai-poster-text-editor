---
title: AI 海报文字快改：项目已完成
updated: 2026-08-26
---

# 项目已完成

## 2026-08-25

- 完成：发布 `v0.2.1`。
- 结果：顶部改为紧凑操作区；新增拖入背景、三种保存位置、JSON5/JavaScript layout 兼容、双语说明和用户真实示例截图。
- 证据：Git 提交 `13af9a3`；GitHub Release `v0.2.1`；GitHub Pages 工作流当时返回 success。
- 验证范围：自动测试覆盖 1440px、900px、旧 GPT JSON、`const layout`、拖入背景、保存、导出和双语；在线页面返回 HTTP 200 并含新标签。
- 回滚点：Git 提交 `37f45ad` / Release `v0.2.0`。

## 2026-08-26

- 完成：本地增强 AI 输入兼容层。
- 结果：安全解析 HTML 空格实体、连续文字对象片段；缺少 `canvas` 时沿用当前画布。未使用代码执行。
- 证据：`test/html-fragment-layout.txt` 与 `test/visual-test.js`；最近测试识别 2 个文字图层及 `1648 × 928` 画布。
- 验证范围：本地自动构建与 Playwright 测试通过；用户实机刷新后的验收尚未确认；尚未提交或发布。
- 回滚点：未提交修改可依据当前 diff 逐文件撤销；禁止使用会影响其他修改的破坏性 Git 命令。

## 2026-08-26（二）

- 完成：导入语义、清空与撤销、会话恢复（原待办 `TASK-APT-009` 代码部分、`TASK-APT-014`、`TASK-APT-015`）。
- 结果：
  - 完整 layout（含 `canvas`）替换全部文字；片段按 `id` 合并：相同 `id` 更新、新 `id` 追加、其他图层保留；导入空 `texts` 数组被明确拒绝。
  - 右侧面板底部新增危险色“清空全部文字”按钮；设置菜单新增“清除上次会话”（仅删除本机 IndexedDB 数据）。
  - 合并、替换、导入、清空、背景替换均为单次原子历史操作，操作后 toast 显示可点击的“撤销”（6 秒）。
  - 删除 `createParchment()` 与默认示例文字；启动时从 IndexedDB 恢复上次背景与排版，无有效会话、存储不可用或数据损坏时显示空白工作区。
  - 历史快照只保存 `{canvas, texts, bgVersion}`，背景 Data URL 存放在独立 `bgAssets` 映射和 IndexedDB `session/background` 键中，不再重复进入每个快照。
- 证据：`index.html`（解析与应用逻辑、IndexedDB v2 `session` store）、`test/visual-test.js` 新增回归。
- 验证范围：`npm run build` 成功；`npm test` exit 0，覆盖空白启动、完整替换（8 层）、片段合并（7→8 层且 prefix/nidana 保留）、空数组拒绝、清空后撤销恢复 8 层、背景替换后撤销（1672×941 且无背景）、刷新后恢复 2×2 背景 + 8 层排版、清除会话后空白启动、IndexedDB 写入损坏数据后回退空白；旧样本 `gpt-layout.txt`、`js-layout.txt`、`html-fragment-layout.txt` 继续通过；`git diff --check` 干净。用户实机验收未做。
- 回滚点：本轮修改集中在 `index.html`、`test/visual-test.js` 及文档，均可依据当前 diff 逐文件撤销。

## 2026-08-26（三）

- 完成：修复背景撤销后重开恢复错误背景。
- 结果：最近会话改为 IndexedDB 单条 `session/current` 原子记录，画布、文字与背景一同写入；撤销与重做同步持久化。清除会话会删除新旧键且不再写回空记录，下一次实际编辑才重新启用持久化。
- 证据：`test/visual-test.js` 精确覆盖“换 2×2 背景 → 撤销为 1672×941 无背景 → 等待写入 → 刷新仍无背景”，随后再次换背景并刷新可恢复；同时确认 `session/current` 在清除后严格不存在，损坏记录会安全回退空白。
- 验证范围：`CI=true pnpm run build` 与 `CI=true pnpm test` 通过；用户实机验收尚未完成；尚未提交或发布。
- 回滚点：本次修复集中在 `index.html`、`test/visual-test.js` 与项目文档，可按当前 diff 定向回退。

## 2026-08-26（四）

- 完成：自动测试与系统 Chrome 隔离。
- 结果：`test/visual-test.js` 不再硬编码启动 `/Applications/Google Chrome.app`，改用 Playwright 专用 Chrome Headless Shell；浏览器关闭放入 `finally`，断言或脚本异常时同样清理。
- 证据：已安装 Playwright `chromium-headless-shell v1234`；`CI=true pnpm test` 使用该路径运行并 exit 0，测试期间未启动系统 Chrome。
- 验证范围：本机自动测试环境；其他开发者首次运行仍需安装对应 Playwright 浏览器。未提交、未发布。
- 回滚点：`test/visual-test.js` 本轮启动与清理逻辑。

## 2026-08-26（五）

- 完成：保存/导出重构与文字设置可读性增强（原 `TASK-APT-011`）。
- 结果：保存排版与导出 PNG 改为紧凑分裂菜单；通过文件选择器导入的 layout 可由主按钮快速覆盖，下拉菜单支持另存、上次文件夹和新文件夹；新增“全部导出”，一键把 PNG 与 layout 写入同一文件夹，不支持目录能力时双下载。字号、字距、行高、X、Y 均有统一步进按钮和 Shift 大步长；属性标签、输入文字和正文输入框字号增大；字体框显示可用、本次加载或可能回退状态。
- 证据：`test/visual-test.js` 覆盖 1440/900px 工具栏、两次快速保存仅一次调起另存选择器、PNG 与 layout 同目录双写、五组步进值及中英文菜单。
- 验证范围：本地无界面 Chromium 测试通过；用户实机视觉与文件系统权限验收尚未完成；未提交、未发布。
- 回滚点：本轮主要集中在 `index.html`、`test/visual-test.js`、README 与使用说明。

## 2026-08-26（六）

- 完成：右侧文字设置一屏化与顶部操作按钮增大。
- 结果：在不缩小字段文字的前提下压缩分组留白、输入高度和说明间距；960px 高窗口可同时看到 X/Y、删除当前文字和清空全部文字。顶部操作按钮从 34px 增至 38px，文字与图标同步增大。
- 证据：端到端断言 `propertyPane.scrollHeight <= clientHeight`，删除与清空按钮均在视口内；1440px 与 900px 工具栏均无溢出。
- 验证范围：本地无界面 Chromium；用户实机视觉验收尚未完成。未提交、未发布。
- 回滚点：`index.html` 属性区与工具栏 CSS、`test/visual-test.js` 对应布局断言。

## 2026-08-26（七）

- 完成：用户授权发布 `v0.3.0`（原 `TASK-APT-009`、`TASK-APT-010`）。
- 结果：界面署名统一为“使用 Codex & 牛来 协助开发”；版本升级至 `0.3.0`，PWA 缓存升级至 v3；本轮兼容性、会话、保存/导出、字体提示、统一步进与一屏布局一并发布。
- 证据：Git tag / GitHub Release `v0.3.0`、Pages 工作流与发布 ZIP。
- 验证范围：`CI=true pnpm run build`、`CI=true pnpm test`、`git diff --check` 均通过；发布后在线 HTTP 与页面署名另行核验。
- 回滚点：GitHub Release `v0.2.1` / Git 提交 `13af9a3`。
