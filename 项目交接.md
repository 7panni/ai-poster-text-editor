# AI 海报文字快改：项目交接

更新时间：2026-08-26

## 一句话目标

让 AI 只生成无文字背景图和结构化文字排版，用户在本地快速修正海报、封面中的错字、内容、字号、颜色和位置，不必为一点文字变化反复重生成整张图片。

## 当前状态

- GitHub 仓库：<https://github.com/7panni/ai-poster-text-editor>
- 在线版：<https://7panni.github.io/ai-poster-text-editor/>
- 最近已发布版本：`v0.3.0`
- 最近已发布提交：见 Git tag `v0.3.0`
- 本地状态：`v0.3.0` 已纳入兼容性增强、导入语义、清空撤销、会话恢复、保存/导出重构和属性区一屏化。开始工作前仍须查看 `git status --short` 和实际 diff。
- 本地新增兼容性已通过自动测试：HTML 实体、连续文字对象片段、缺少 `canvas` 时沿用当前画布；片段按 `id` 合并、清空撤销、背景撤销、刷新恢复、清除会话与损坏回退均有端到端回归。

## 技术栈

| 层 | 技术与约定 |
|---|---|
| 应用 | 单页 `index.html`，原生 HTML、CSS、JavaScript，无前端框架 |
| 绘制 | Canvas 2D；背景图与文字图层实时合成，PNG 按原画布尺寸导出 |
| 源码编辑 | CodeMirror 6；源码在 `src/source-editor.js`，由 esbuild 打包到 `vendor/source-editor.js` |
| 输入解析 | 标准 JSON + JSON5；兼容 Markdown 围栏、JavaScript 外壳、字段别名、HTML 空格实体和文字对象片段 |
| 本地文件 | File API、File System Access API、IndexedDB；不支持时回退为浏览器下载 |
| 字体 | FontFace API 加载本机 TTF、OTF、WOFF、WOFF2，不上传 |
| 离线 | PWA：`manifest.webmanifest` + `sw.js`；当前缓存名为 `ai-poster-text-editor-v2` |
| 构建 | pnpm + esbuild；Node 依赖见 `package.json` 与 `pnpm-lock.yaml` |
| 测试 | Playwright 专用无界面 Chromium；主入口 `test/visual-test.js` |
| 发布 | GitHub Pages + GitHub Releases ZIP；发布包不进入 Git，`release/` 被忽略 |

## 核心数据模型

标准排版结构：

```json
{
  "canvas": { "width": 1600, "height": 900 },
  "texts": [
    {
      "id": "title",
      "content": "主标题",
      "x": 1120,
      "y": 390,
      "font": "Noto Serif SC",
      "size": 150,
      "weight": "700",
      "color": "#344022",
      "letter_spacing": 10,
      "line_height": 1.2,
      "align": "center",
      "visible": true
    }
  ]
}
```

`x`、`y` 是画布像素坐标，`y` 是文字基线。AI 常给出文本框模型：`x`、`y` 为左上角，另有 `width` 与 `fontSize`。当前转换规则为：居中时 `x += width / 2`，右对齐时 `x += width`，基线 `y += fontSize`。

只有文字对象片段而没有 `canvas` 时，当前实现沿用现有画布尺寸，并按 `id` 合并：相同 `id` 更新、未知 `id` 新增、其他图层保留。完整 layout（含 `canvas`）替换全部文字。导入空 `texts` 数组会被拒绝；清除文字只能通过右侧面板底部的“清空全部文字”按钮。

## 文件地图

- `index.html`：主要界面、画布、编辑状态、文件保存、导入归一化和大部分业务逻辑。
- `src/source-editor.js`：CodeMirror、JSON5 安全解析和编辑器 lint。
- `vendor/source-editor.js`：构建产物，不直接编辑。
- `test/visual-test.js`：端到端回归，包括两类完整 layout、HTML 实体片段、拖入背景、保存、双语、窄窗口。
- `test/gpt-layout.txt`：旧 GPT JSON 回归样本。
- `test/js-layout.txt`：`const layout`、JSON5、Markdown 围栏和文本框坐标样本。
- `test/html-fragment-layout.txt`：HTML 实体和连续对象片段样本。
- `docs/screenshot-zh.png`、`docs/screenshot-en.png`：用户真实海报示例，不是测试夹具。
- `README.md`、`使用说明.md`：对外中英文说明与 AI 生成提示词。
- `项目待办.md`：唯一当前任务来源。

## 目标边界

当前产品是本地网页/PWA，不是原生桌面应用。Windows、macOS、Linux 可用浏览器运行；手机基础可用，精细排版仍以桌面为主。浏览器不会暴露普通上传文件的真实路径；保存菜单只能使用文件句柄、用户授权的输出文件夹或浏览器下载，不能伪装成自动识别图片原文件夹。

启动时不再调用 `createParchment()`（已删除）。程序优先从 IndexedDB 的单条 `session/current` 记录原子恢复画布、文字与背景；没有有效会话、存储不可用或用户主动清除会话时显示空白工作区和“拖入图片或替换背景”的引导。内存历史快照只含 `{canvas, texts, bgVersion}`，背景资源由 `bgAssets` 映射复用；持久化时背景与排版不会拆开，因此背景撤销后重开不会错配。旧版 `session/layout` 与 `session/background` 会迁移到新记录。合并、替换、导入、清空和背景替换都是单次原子历史操作，操作后显示可点击“撤销”的 toast；“设置”里有“清除上次会话”，清除后不会写回空会话，直到用户再次编辑。数据只保存在本机。

## 禁止事项

1. 禁止执行用户粘贴的 JavaScript；不得用 `eval` 或 `new Function` 追求兼容性。
2. 禁止把图片、字体、layout 或用户路径发送到网络、分析服务或后端。
3. 禁止引入运行时 CDN；成品必须可离线打开。
4. 禁止直接修改构建产物而不修改源码和重建。
5. 禁止把文字烘焙进示例背景图作为“可编辑文字”。
6. 禁止把浏览器不具备的路径、覆盖或跨平台能力写成已实现。
7. 禁止未经确认恢复暂停项：Tauri 原生安装包。保存/导出分裂按钮已由用户在 2026-08-26 明确恢复并完成本地实现。
8. 禁止让自动测试覆盖真实文档截图。
9. 禁止把本地测试通过写成用户已验收、Pages 已部署或 Release 已发布。
10. 禁止覆盖、回退或提交来源不明的工作区修改。

## 构建与验证

```bash
CI=true pnpm install --no-frozen-lockfile
CI=true pnpm run build
CI=true pnpm test
git diff --check
```

通常无需每次重新安装依赖。首次需要运行 `node_modules/.bin/playwright install --only-shell chromium` 安装专用无界面浏览器；`pnpm test` 不再启动系统 Google Chrome，并在成功或失败时都关闭浏览器。受沙箱限制时仍可能需要明确申请权限。发布前还要人工打开 `index.html`，用真实背景图检查文字选择、空白取消选择、双语、保存与导出。

## 当前关键决策

- 产品名：中文“AI 海报文字快改”，英文“AI Poster Text Editor”。
- 开发者：尚宸鸣 / `Vijjādassī Shang`；使用 Codex & 牛来 协助开发。
- 中文默认，英文可切换。
- 左侧文字图层列表已取消；在画布上直接选择文字。
- `layout.json` 源码始终显示在右侧标签页，不切换整页。
- 完整 layout 负责替换，文字对象片段按 `id` 合并；清空全部文字是右侧面板底部的独立危险操作。已实现并有回归测试。
- 合并、替换、导入、清空和背景替换形成单次原子历史记录，支持撤销/重做；操作后提供可点击“撤销”提示。最近会话（排版 + 背景）保存在本机 IndexedDB，刷新后恢复；完整撤销栈不跨刷新持久化。
- 启动策略已实现为“恢复上次会话，否则空白”；内置羊皮纸已删除。
- 保存/导出已重构为紧凑分裂菜单；导入的可写 layout 可快速覆盖，菜单可另存或选择输出文件夹；“全部导出”把 PNG 与 layout 写入同一文件夹，不支持文件夹权限时回退为双下载。
- 字号、字距、行高、X、Y 使用统一步进控件，Shift 点击为大步长；字体框提示可用、当次加载或可能回退。
- 960px 高窗口中，右侧应无需滚动即可同时看到 X/Y、删除当前文字与底部清空按钮；顶部操作按钮为 38px 高，900px 宽度仍需保持不溢出。
- 原生 Tauri 套壳与安装包暂停，欢迎贡献者接手，但不能标记为当前已支持。

## 接手建议

先处理 `项目待办.md` 中唯一的“当前”任务，不要同时启动 Tauri 和新格式兼容。每新增一种 AI 输入格式，必须保存原始失败样本并加入端到端测试；不要只为一个样本写不可解释的正则补丁。

## Codex 与 OpenCode 协作

- 同一个工作目录只允许一个 Agent 在同一时间写代码；另一个可以只读审查。当前主要逻辑集中在 `index.html`，并行编辑极易互相覆盖。
- 真正并行时，为每个任务创建独立 Git branch + worktree；完成后由主工作区统一审查和合并。不要让两个 Agent 共享未提交的 `index.html`。
- `项目待办.md` 仍是唯一当前任务来源。开始前写清任务 ID、允许修改文件、验收标准；完成事实写入 `项目已完成.md`，版本快照只增不改。
- 每次交接必须提供：`git status --short`、实际修改文件、测试命令与结果、未验证风险、是否提交/推送/发布。接手者先读本文件、待办、已完成和 `AGENTS.md`，再查看 diff。
- 当前工作区有大量未提交修改。在形成一次用户验收后的基线提交前，建议 Codex 与 OpenCode 串行工作，不要立即并行。
