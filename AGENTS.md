# AGENTS.md

## 开始工作前

1. 完整阅读 `项目交接.md`、`项目待办.md`、`项目已完成.md`。
2. `项目待办.md` 是唯一当前待办；不要从 README、聊天记录或版本快照推断进度。
3. 先运行 `git status --short`。当前可能存在用户或其他代理留下的未提交修改，不得覆盖、丢弃或回退。

## 项目目标

维护“AI 海报文字快改”：一个本地优先、无需后端、默认中文的双语工具。它把无文字背景图与可编辑文字图层分离，让用户不用反复生成整张 AI 图片，就能修改文字内容、字体、字号、颜色和位置。

## 修改规则

- 修改前说明计划；只改当前任务所需文件。
- 不使用 `eval`、`new Function` 或其他方式执行用户粘贴的代码。AI 输出只能经过安全解析和结构归一化。
- 图片、字体和排版数据不得上传；运行时不得依赖 CDN 或后端。
- 不手改 `vendor/source-editor.js`。修改 `src/source-editor.js` 后运行 `pnpm run build`。
- 不把测试截图写入 `docs/screenshot-zh.png` 或 `docs/screenshot-en.png`；这两张图保留用户真实示例。
- 不静默覆盖用户文件。浏览器文件夹与原路径能力必须按真实限制描述。
- 默认中文与中英文切换必须保留；作者英文统一为 `Vijjādassī Shang`。
- Tauri 原生套壳与新的保存/导出交互目前暂停，除非用户明确恢复。
- 完整测试通过也不等于已发布。未经用户明确授权，不提交、推送、创建 Release 或改 GitHub Pages。
- 禁止破坏性 Git 操作；不得清理或回退不属于当前任务的修改。

## 完成要求

- 运行 `CI=true pnpm run build` 与 `CI=true pnpm test`；Chrome 启动受限时申请权限，不要删减测试绕过失败。
- 运行 `git diff --check`。
- 更新 `项目待办.md`；已确认结果移入 `项目已完成.md`，写明证据、验证范围和回滚点。
- 交付时分别报告：本地代码、自动测试、用户实机验证、GitHub 发布状态。
