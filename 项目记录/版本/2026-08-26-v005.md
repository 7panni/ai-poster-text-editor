# v005 本地快照：无干扰自动测试

日期：2026-08-26
状态：本地完成，未提交、未发布

自动测试改用 Playwright 专用 Chrome Headless Shell，不再启动用户日常使用的 Google Chrome。浏览器生命周期由 `try/finally` 兜底，测试成功、断言失败或中途异常都会尝试关闭。

本机已安装 `chromium-headless-shell v1234`，`CI=true pnpm test` 通过。其他开发者首次运行需执行：

```bash
node_modules/.bin/playwright install --only-shell chromium
```
