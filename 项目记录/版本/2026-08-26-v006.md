# v006 本地快照：快速保存、全部导出与统一微调

日期：2026-08-26
状态：本地完成，未提交、未发布，待用户实机验收

顶部保存与导出改为紧凑分裂菜单，并新增“全部导出”。可写 layout 支持快速覆盖，PNG 与 layout 可一键写入同一授权文件夹；目录 API 不可用时回退为浏览器下载。

文字设置区增大标签、输入值和正文编辑字号。字号、字距、行高、X、Y 均改为 `− / 数值 / ＋`，Shift 点击使用大步长。字体输入保留 AI 字体名兼容，同时提示可用、本次加载或可能显示替代字体。

自动回归覆盖双文件导出、快速覆盖、菜单中英文、五组步进与 900px 顶栏适配。GitHub 尚未提交或发布。
