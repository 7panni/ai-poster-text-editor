# v008 发布快照：v0.3.0

日期：2026-08-26
状态：用户已授权发布

`v0.3.0` 汇总发布：AI layout 兼容与按 ID 合并、清空及撤销、原子会话恢复、Playwright 无界面测试、保存/导出分裂菜单、PNG 与 layout 一键双导出、字体可用提示、字号/字距/行高/X/Y 统一步进、右侧属性一屏化。

署名统一为：开发者尚宸鸣 / Vijjādassī Shang；使用 Codex & 牛来 协助开发。

发布前验证：`CI=true pnpm run build`、`CI=true pnpm test`、`git diff --check` 全部通过。PWA 缓存名升级为 `ai-poster-text-editor-v3`。
