# AI 海报文字快改 2026-08-26-v003

日期：2026-08-26

## 本版变化

- 导入语义落地：完整 layout（含 `canvas`）替换全部文字；文字对象片段按 `id` 合并，相同 `id` 更新、未知 `id` 新增、其他图层保留。
- 导入空 `texts` 数组被明确拒绝并提示使用“清空全部文字”按钮。
- 右侧面板底部新增危险色“清空全部文字”按钮；设置菜单新增“清除上次会话”（仅删除本机数据）。
- 合并、替换、导入、清空和背景替换均作为单次原子操作进入撤销栈；操作后显示带可点击“撤销”的 toast（6 秒）。
- 移除 `createParchment()` 与默认示例文字；启动时优先从 IndexedDB（v2 `session` store）恢复上次背景与排版；无有效会话、存储不可用或数据损坏时显示空白工作区。
- 历史快照瘦身为 `{canvas, texts, bgVersion}`；背景 Data URL 保存在独立的 `bgAssets` 映射与 IndexedDB `session/background` 键中，不再随每个快照重复存储。

## 当前状态

- 自动测试 exit 0：空白启动、完整替换（8 层）、片段合并（7→8 层）、空数组拒绝、清空撤销、背景撤销、刷新恢复、清除会话空白启动、IndexedDB 损坏回退，以及既有三份样本（gpt / js / html-fragment）全部通过。
- 未提交、未发布；等待用户实机验收。

## 未解决

- 用户尚未实机确认：真实失败样本导入、清空/撤销手感、刷新恢复、清除会话入口。
- 撤销栈仍限内存内 60 步，跨刷新只恢复“最近会话”，不恢复完整历史。
- `bgAssets` 版本回收依赖历史引用裁剪；极端频繁换背景时的内存占用未做上限压测。

## 验证范围

- `npm run build` 成功；`npm test`（Playwright + Chrome headless）exit 0；`git diff --check` 干净。
- 本机无 pnpm，使用等效 npm 脚本。

## 回滚点

- 本轮改动集中于 `index.html`、`test/visual-test.js` 与文档文件；依据当前 diff 逐文件撤销即可，不涉及已发布提交。
