# AI 海报文字快改 2026-08-26-v001

日期：2026-08-26

## 本版变化

- 初始化项目交接与 Markdown 状态体系。
- 记录已发布 `v0.2.1` 与本地未发布兼容性增强之间的边界。
- 将保存/导出分裂按钮和 Tauri 套壳列为暂停项。

## 当前状态

- 唯一当前待办：根目录 `项目待办.md`。
- 当前里程碑：`M-003` 稳定 AI 排版输入兼容层。
- 当前下一步：用户实机复核 HTML 实体片段，并决定片段替换或合并语义。

## 未解决

- 本地兼容性增强尚未提交、推送或发布。
- 单独文字片段当前会替换所有文字图层，尚未获得用户对该语义的确认。
- 保存/导出交互重构和 Tauri 原生套壳均暂停。

## 验证范围

- 最近本地构建和 Playwright 端到端测试通过。
- GitHub 线上基线仍为 `v0.2.1` / `13af9a3`。
- 本快照只记录事实，不代表用户已完成实机验收。

## 回滚点

- 已发布稳定点：Git 提交 `13af9a3`。
- 上一发布点：Git 提交 `37f45ad` / `v0.2.0`。
- 当前工作区含未提交修改，回滚必须逐文件核对，不得使用 `git reset --hard` 或批量 checkout。
