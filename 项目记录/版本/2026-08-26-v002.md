# AI 海报文字快改 2026-08-26-v002

日期：2026-08-26

## 本版变化

- 确认导入语义：完整 layout 替换全部文字，文字对象片段按 `id` 合并。
- 规划独立“清空全部文字”按钮和操作后撤销。
- 确认启动策略：优先恢复上次会话，否则显示空白工作区；移除内置羊皮纸默认图。

## 当前状态

- 当前程序仍会通过 `createParchment()` 生成默认羊皮纸和默认文字。
- 当前 HTML 实体与对象片段可以解析，但片段仍会替换全部图层，尚未实现按 `id` 合并。
- 当前有内存撤销/重做，刷新后的持久恢复尚未实现。

## 未解决

- 需要设计 IndexedDB 会话结构，避免在每个历史快照中重复保存大体积背景 Data URL。
- 需要决定持久恢复点的数量和空间上限；建议一个完整“上次会话”加有限数量的排版快照。
- 清空、合并、替换、背景替换必须验证为原子操作。

## 验证范围

- 本次只更新项目文档，没有修改或测试功能代码。
- `createParchment()` 与当前初始化调用已在源码中核对。

## 回滚点

- 删除本快照并恢复本次对 `项目交接.md`、`项目待办.md` 的文档修改即可；不涉及代码回滚。
