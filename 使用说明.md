# AI海报文字快改 / AI Poster Text Editor

很多人用 AI 生成海报或封面后，会遇到错字、乱码或排版问题。即使文字没有错，只想改内容、字号、颜色或位置，也常常要反复生成半天，背景和构图还可能跟着改变。

AI 海报文字快改把背景图和文字排版分离：用无文字背景图搭配 AI 给出的 `layout.json`，导入后直接点击文字修改，几秒钟即可重新导出，不必重生成整张图片。图片和字体只在本地处理。

AI-generated posters often contain text errors, and even a tiny wording, size, color, or position change can take many regenerations. This editor separates the text-free background from `layout.json`, so the text can be corrected locally without regenerating the artwork.

完整的中英文说明，以及可直接复制给 AI 的“背景图 + layout.json + 设计文档”提示词，见 [README.md](README.md)。

For the complete bilingual guide and copy-ready prompts for generating a background image, `layout.json`, and design notes, see [README.md](README.md).

## 使用与安装

- 本地成品：下载 ZIP，解压后打开 `index.html`。
- 在线/PWA：[7panni.github.io/ai-poster-text-editor](https://7panni.github.io/ai-poster-text-editor/)
- 命令行与 AI 安装提示词：[README 使用与安装](README.md#使用与安装--use-and-installation)

点“替换背景”或直接把图片拖到画布，再“导入排版”或把 AI 代码粘贴到右侧 `layout.json`。点击画布文字即可调整；完成后保存排版或导出 PNG。“设置”可选择浏览器默认、上次使用或图片同文件夹。浏览器无法读取图片真实路径，所以“图片同文件夹”首次需要手动确认文件夹。

当前没有 `.exe`、`.dmg`、`.AppImage` 原生安装包。项目可以用 Tauri 继续开发三平台安装包，但这项工作目前暂停；欢迎有空的开发者接手并提交 Pull Request。

开发者：尚宸鸣 ｜ 使用 Codex 协助开发 ｜ [项目地址](https://github.com/7panni/ai-poster-text-editor) ｜ [作者博客](https://7panni.com)

## 粘贴 AI 给你的 layout.json

1. 点右侧“layout.json”。图片会继续显示在左侧。
2. 全选现有代码，粘贴 AI 给你的 `layout.json`。
3. 底部显示“格式正确”后，点“应用到画布”。
4. 如果 JSON 有语法错误，会显示错误位置并禁止应用，不会破坏当前画布。

编辑器会自动清除 Markdown 代码围栏和错误的 `\_` 转义，也能识别 GPT 常见的 `text/content`、`font_family/font`、`font_size/size`、`font_weight/weight`、`anchor/align` 等写法。点“格式化”可统一转换为标准 `layout.json`。源码编辑使用 MIT 许可的 CodeMirror 6，已随程序打包，运行时不访问 CDN。

## 注意

背景图必须是不带文字的纯背景。如果文字已经烘焙进图片，编辑器无法自动擦除；应先使用纯背景，再把文字作为图层加入。
