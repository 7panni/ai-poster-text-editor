# AI海报文字快改 / AI Poster Text Editor

打开 `index.html` 即可使用，不需要安装。

Open `index.html` to use the app. No installation is required.

完整的中英文说明、平台兼容表，以及可直接复制给 AI 的中英文“背景图 + layout.json + 设计文档”提示词，见 [README.md](README.md)。

For the complete bilingual guide, platform compatibility table, and copy-ready Chinese/English prompts for generating a background image, `layout.json`, and design notes, see [README.md](README.md).

## 安装入口

- 本地成品：下载 ZIP，解压后打开 `index.html`。
- 在线/PWA：[7panni.github.io/ai-poster-text-editor](https://7panni.github.io/ai-poster-text-editor/)
- 命令行与 AI 安装提示词：[README 安装方式](README.md#安装方式--installation)
- 当前没有 `.exe`、`.dmg`、`.AppImage` 原生安装包。

开发者：尚宸鸣 ｜ 使用 Codex 协助开发 ｜ [项目地址](https://github.com/7panni/ai-poster-text-editor) ｜ [作者博客](https://7panni.com)

## 最短流程

1. 点“换背景”，选择一张不带文字的背景图。
2. 直接点击画布中的文字，右侧修改内容、字体、字号、颜色与位置。
3. 文字可直接拖动；点击空白处或按 `Esc` 取消选择。
4. 点“导出 PNG”获得原图尺寸封面。
5. 点“保存排版”保存 `layout.json`，下次可继续导入。

## 保存位置

- 点“输出文件夹”选择一次文件夹，之后排版和 PNG 都直接写入该文件夹。
- 未选择输出文件夹时，“保存排版”和“导出 PNG”会打开系统保存窗口，可手动选择路径。
- 文件名根据背景图生成：`背景名-layout.json`、`背景名-cover.png`。
- 浏览器出于隐私保护不会提供上传文件的真实路径，因此纯网页不能自动识别背景图所在文件夹。若要保存到同一处，请把“输出文件夹”选为背景图文件夹。

## 粘贴 AI 给你的 layout.json

1. 点右侧“layout.json”。图片会继续显示在左侧。
2. 全选现有代码，粘贴 AI 给你的 `layout.json`。
3. 底部显示“格式正确”后，点“应用到画布”。
4. 如果 JSON 有语法错误，会显示错误位置并禁止应用，不会破坏当前画布。

编辑器兼容 GPT 常见输出：

- 自动清除 ` ```json ` 代码围栏和错误的 `\_` 转义。
- 同时识别 `text/content`、`font_family/font`、`font_size/size`、`font_weight/weight`、`anchor/align`。
- 点“格式化”可把兼容格式统一转换为本编辑器的标准 `layout.json`。

## 已支持

- 任意比例背景图，自动按原图尺寸导出
- 多文字图层、添加/删除
- 多行文字、字距、行高、对齐、颜色、字重
- 拖动定位与方向键微调（`Shift` + 方向键为 10 px）
- 撤销/重做（`⌘Z` / `⇧⌘Z`）
- 加载本机 TTF、OTF、WOFF 字体
- 导入此前约定的 `layout.json`
- CodeMirror 6 源码模式：JSON 高亮、括号匹配、错误标记、格式化
- 所有图片与字体只在本机浏览器处理，不上传

## 开源组件

- CodeMirror 6（MIT）：用于 `layout.json` 源码编辑。
- 应用已打包为本地文件，运行时不访问 CDN。

## 注意

背景图必须是不带文字的纯背景。如果文字已经烘焙进图片，编辑器无法自动擦除；应先使用纯背景，再把文字作为图层加入。
